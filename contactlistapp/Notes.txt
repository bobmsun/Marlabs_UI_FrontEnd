Express is a middleware. 它有点像 web server. It helps us to create web application.

npm - node package manager, 有点像 maven

如果把 node_modules 这个folder删掉（把所有的libraray 删掉），只要cd 到 package.json 所在的文件夹，run 一下 npm install, 就可以把 package 安装回来；
如果从 git 上pull 下来老师的project，然后想run；需要先 run 一下 npm install 把所有需要的package 下载下来，因为老师的 gitignore 把 node_modules 给 ignore 了


如果自己的电脑 set up 不了 mongoDB，可以用 mongoDB atlas. MongoDB atlas 是 online Mongo MongoDB, online database.


端口：
MongoDb 20017
MySQL  3306
Tomcat 8080


what are the ways to create JS objects?
4 ways


MongoDB 中，先 create database，再 create Collection (有点像 SQL Database 中的 table), 再 insert document (就是 SQL 中table 的 row)



In higher version of Angular 11, eg Angular 11, 用 mongoose (类似与hibernate)
For AngularJS, we have to use mongojs

We're using Express server. Express is a server for node js. If you want to use a package in JS file, you need to require it.
Require is a JS built-in function. Require function loads the package.

Express is a web server for Node js.

